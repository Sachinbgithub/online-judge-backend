### Test TestCase API Endpoints

### Variables
@baseUrl = http://localhost:5081

### 1. Get all test cases
GET {{baseUrl}}/api/TestCase

### 2. Get test case by ID
GET {{baseUrl}}/api/TestCase/1

### 3. Get test cases by problem ID
GET {{baseUrl}}/api/TestCase/problem/1

### 4. Create a new test case for problem 1
POST {{baseUrl}}/api/TestCase
Content-Type: application/json

{
  "problemId": 1,
  "input": "[2,7,11,15]",
  "expectedOutput": "[0,1]"
}

### 5. Create another test case for problem 1
POST {{baseUrl}}/api/TestCase
Content-Type: application/json

{
  "problemId": 1,
  "input": "[3,2,4]",
  "expectedOutput": "[1,2]"
}

### 6. Create a test case for problem 2
POST {{baseUrl}}/api/TestCase
Content-Type: application/json

{
  "problemId": 2,
  "input": "[\"h\",\"e\",\"l\",\"l\",\"o\"]",
  "expectedOutput": "[\"o\",\"l\",\"l\",\"e\",\"h\"]"
}

### 7. Create a test case with null input/output (should work since they're nullable)
POST {{baseUrl}}/api/TestCase
Content-Type: application/json

{
  "problemId": 1,
  "input": null,
  "expectedOutput": null
}

### 8. Update test case (replace ID with actual test case ID)
PUT {{baseUrl}}/api/TestCase/1
Content-Type: application/json

{
  "input": "[1,2,3,4,5]",
  "expectedOutput": "[0,1]"
}

### 9. Test validation - invalid problem ID
POST {{baseUrl}}/api/TestCase
Content-Type: application/json

{
  "problemId": 999,
  "input": "test",
  "expectedOutput": "test"
}

### 10. Test validation - missing required field
POST {{baseUrl}}/api/TestCase
Content-Type: application/json

{
  "input": "test",
  "expectedOutput": "test"
}

### 11. Test validation - input too long (over 255 characters)
POST {{baseUrl}}/api/TestCase
Content-Type: application/json

{
  "problemId": 1,
  "input": "This is a very long input string that exceeds the maximum length of 255 characters allowed by the database schema. This string should be rejected by the validation because it contains more than 255 characters which is the limit set in the database schema for the Input field. Let me add some more text to make sure it definitely exceeds the limit and triggers the validation error that we expect to see when testing the API endpoint for creating test cases with invalid data.",
  "expectedOutput": "test"
}

### 12. Delete specific test case (replace ID with actual test case ID)
DELETE {{baseUrl}}/api/TestCase/1

### 13. Delete all test cases for a problem
DELETE {{baseUrl}}/api/TestCase/problem/1

### 14. Get test cases for non-existent problem
GET {{baseUrl}}/api/TestCase/problem/999
